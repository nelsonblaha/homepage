# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Admin password for the /admin panel
ADMIN_PASSWORD=changeme

# Session secret for secure cookies (generate with: openssl rand -hex 32)
SESSION_SECRET=

# Database path (inside container)
DB_PATH=/app/data/homepage.db

# =============================================================================
# DOMAIN CONFIGURATION (Required for production)
# =============================================================================

# Your base domain (e.g., "example.com")
# Used for generating service URLs, emails, and redirects
BASE_DOMAIN=localhost

# Cookie domain (e.g., ".example.com" - note the leading dot)
# Leave empty for localhost development
COOKIE_DOMAIN=

# Admin email for forward auth headers
ADMIN_EMAIL=admin@localhost

# =============================================================================
# OPTIONAL: BASIC AUTH (nginx level protection)
# =============================================================================

# Optional basic auth credentials for nginx (adds extra security layer)
BASIC_AUTH_USER=
BASIC_AUTH_PASS=

# =============================================================================
# OPTIONAL: PLEX INTEGRATION (Auto-account creation)
# =============================================================================

# Get your Plex token from: https://plex.tv/devices.xml
# Look for token= attribute in your server's entry
PLEX_TOKEN=

# Your Plex server URL
# Use localhost:port or your server's hostname/IP
PLEX_URL=http://localhost:32400

# =============================================================================
# OPTIONAL: OMBI INTEGRATION (Auto-account creation)
# =============================================================================

# Get API key from: Ombi > Settings > Configuration > General
OMBI_URL=http://localhost:3579
OMBI_API_KEY=

# =============================================================================
# OPTIONAL: JELLYFIN INTEGRATION (Auto-account creation)
# =============================================================================

# Get API key from: Jellyfin > Dashboard > Administration > API Keys
JELLYFIN_URL=http://localhost:8096
JELLYFIN_API_KEY=

# =============================================================================
# OPTIONAL: OVERSEERR INTEGRATION (Auto-account creation)
# =============================================================================

# Get API key from: Overseerr > Settings > General
OVERSEERR_URL=http://localhost:5055
OVERSEERR_API_KEY=

# =============================================================================
# OPTIONAL: NEXTCLOUD INTEGRATION (Auto-account creation)
# =============================================================================

NEXTCLOUD_URL=http://localhost:8080
NEXTCLOUD_ADMIN_USER=
NEXTCLOUD_ADMIN_PASS=

# =============================================================================
# OPTIONAL: MATTERMOST INTEGRATION (Auto-account creation)
# =============================================================================

MATTERMOST_URL=http://localhost:8065
MATTERMOST_TOKEN=
MATTERMOST_TEAM_ID=

# =============================================================================
# OPTIONAL: TWILIO SMS 2FA
# =============================================================================

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# =============================================================================
# OPTIONAL: NGINX INTEGRATION (htpasswd management)
# =============================================================================

# Path to nginx htpasswd file (must be writable by the app)
NGINX_HTPASSWD_PATH=

# Command to reload nginx after htpasswd changes
# Examples: "docker exec nginx nginx -s reload" or "systemctl reload nginx"
NGINX_RELOAD_CMD=

# Command to test nginx config
NGINX_TEST_CMD=
